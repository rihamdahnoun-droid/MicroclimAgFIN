% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_microclimate_map.R
\name{plot_microclimate_map}
\alias{plot_microclimate_map}
\title{Plot Microclimate Maps}
\usage{
plot_microclimate_map(
  lst = NULL,
  ndvi = NULL,
  anomaly = NULL,
  hot_zones = NULL,
  thermal_zones = NULL,
  map_type = "all",
  title = "Microclimate Analysis",
  output_dir = "outputs/maps",
  export = "png",
  width = 10,
  height = 8
)
}
\arguments{
\item{lst}{SpatRaster. LST raster from clean_lst_data(). Default is NULL.}

\item{ndvi}{SpatRaster. NDVI raster from calculate_ndvi(). Default is NULL.}

\item{anomaly}{SpatRaster. Anomaly raster from calculate_lst_anomaly(). Default is NULL.}

\item{hot_zones}{SpatRaster. Hot zones raster from calculate_lst_anomaly(). Default is NULL.}

\item{thermal_zones}{SpatRaster. Thermal zones from cluster_thermal_zones(). Default is NULL.}

\item{map_type}{Character. Type of map to produce:
"lst", "ndvi", "anomaly", "hot_zones", "thermal_zones", or "all".
Default is "all".}

\item{title}{Character. Main title for the map. Default is "Microclimate Analysis".}

\item{output_dir}{Character. Directory to save maps. Default is "outputs/maps".}

\item{export}{Character. Export format: "png", "pdf", or "none". Default is "png".}

\item{width}{Numeric. Width of exported figure in inches. Default is 10.}

\item{height}{Numeric. Height of exported figure in inches. Default is 8.}
}
\value{
Invisibly returns a list of ggplot objects.
}
\description{
Creates publication-ready microclimate maps from LST, NDVI, thermal
anomalies, and heat island data. Supports export to PNG or PDF and
multi-date faceting.
}
\examples{
\dontrun{
plot_microclimate_map(
  lst        = lst_clean,
  ndvi       = ndvi,
  anomaly    = anomalies$anomaly,
  hot_zones  = anomalies$hot_zones,
  map_type   = "all",
  export     = "png"
)
}

}
