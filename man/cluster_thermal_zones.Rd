% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_thermal_zones.R
\name{cluster_thermal_zones}
\alias{cluster_thermal_zones}
\title{Cluster Thermal Zones}
\usage{
cluster_thermal_zones(
  lst,
  features = NULL,
  n_clusters = 4,
  vars = c("lst_mean", "ndvi_mean", "elevation"),
  output_dir = "outputs/maps",
  save = TRUE
)
}
\arguments{
\item{lst}{SpatRaster. Cleaned LST raster from clean_lst_data().}

\item{features}{Data frame. Microclimate features from extract_microclimate_features().}

\item{n_clusters}{Numeric. Number of thermal clusters. Default is 4.}

\item{vars}{Character vector. Variables to use for clustering.
Default is c("lst_mean", "ndvi_mean", "elevation").}

\item{output_dir}{Character. Directory to save results. Default is "outputs/maps".}

\item{save}{Logical. Whether to save results. Default is TRUE.}
}
\value{
A list containing:
\describe{
\item{raster}{SpatRaster with thermal zone classification}
\item{clusters}{Data frame with cluster assignments}
\item{centers}{Data frame with cluster centers}
\item{stats}{Summary statistics per cluster}
}
}
\description{
Classifies the study area into thermal zones (cool, moderate, warm, hot)
using K-means clustering on LST data. Helps identify spatial patterns
of temperature distribution in agricultural landscapes.
}
\examples{
\dontrun{
thermal_zones <- cluster_thermal_zones(
  lst      = lst_clean,
  features = features,
  n_clusters = 4
)
}

}
