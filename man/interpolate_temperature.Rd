% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpolate_temperature.R
\name{interpolate_temperature}
\alias{interpolate_temperature}
\title{Interpolate Temperature from Weather Stations}
\usage{
interpolate_temperature(
  weather_data,
  stations_sf,
  lst,
  method = "idw",
  date = NULL,
  output_dir = "outputs/maps",
  save = TRUE
)
}
\arguments{
\item{weather_data}{Data frame. Weather data from import_weather_data().}

\item{stations_sf}{SF object. Station locations from import_station_locations().}

\item{lst}{SpatRaster. LST raster used as spatial template.}

\item{method}{Character. Interpolation method: "idw" or "kriging". Default is "idw".}

\item{date}{Character. Date to interpolate (YYYY-MM-DD). If NULL, uses mean temperature.}

\item{output_dir}{Character. Directory to save results. Default is "outputs/maps".}

\item{save}{Logical. Whether to save the raster. Default is TRUE.}
}
\value{
A SpatRaster with interpolated temperature surface.
}
\description{
Spatially interpolates air temperature measurements from weather stations
to produce a continuous temperature surface. Supports IDW (Inverse Distance
Weighting) and simple Kriging methods.
}
\examples{
\dontrun{
# IDW interpolation
temp_surface <- interpolate_temperature(
  weather_data = weather,
  stations_sf  = stations,
  lst          = lst_clean,
  method       = "idw"
)

# Kriging interpolation
temp_surface <- interpolate_temperature(
  weather_data = weather,
  stations_sf  = stations,
  lst          = lst_clean,
  method       = "kriging"
)
}

}
