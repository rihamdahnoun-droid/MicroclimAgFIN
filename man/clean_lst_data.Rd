% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_lst_data.R
\name{clean_lst_data}
\alias{clean_lst_data}
\title{Clean and Preprocess LST Data}
\usage{
clean_lst_data(
  lst,
  temp_min = -20,
  temp_max = 60,
  fill_na = TRUE,
  target_res = NULL,
  output_dir = "outputs/maps",
  save = TRUE
)
}
\arguments{
\item{lst}{SpatRaster. LST raster object returned by download_lst_data().}

\item{temp_min}{Numeric. Minimum valid temperature in Celsius. Default is -20.}

\item{temp_max}{Numeric. Maximum valid temperature in Celsius. Default is 60.}

\item{fill_na}{Logical. Whether to fill NA values by interpolation. Default is TRUE.}

\item{target_res}{Numeric. Target resolution in degrees. NULL keeps original. Default is NULL.}

\item{output_dir}{Character. Directory to save cleaned raster. Default is "outputs/maps".}

\item{save}{Logical. Whether to save the cleaned raster. Default is TRUE.}
}
\value{
A cleaned SpatRaster object.
}
\description{
Cleans Land Surface Temperature (LST) raster data by removing outliers,
handling missing values (NA), harmonizing resolution, and applying
simple interpolation to fill gaps.
}
\examples{
\dontrun{
lst_raw     <- download_lst_data(country="MAR", region="Souss-Massa",
                                  year=2022, month=c(6,7,8))
lst_clean   <- clean_lst_data(lst_raw)
}

}
