% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_ndvi.R
\name{calculate_ndvi}
\alias{calculate_ndvi}
\title{Calculate NDVI from Remote Sensing Data}
\usage{
calculate_ndvi(
  country = NULL,
  region = NULL,
  year = NULL,
  month = NULL,
  output_dir = "outputs/maps",
  save = TRUE
)
}
\arguments{
\item{country}{Character. ISO3 country code. Default is NULL (interactive).}

\item{region}{Character. Region name. Default is NULL (interactive).}

\item{year}{Numeric. Year of interest. Default is NULL (interactive).}

\item{month}{Numeric vector. Months to process (1-12). Default is NULL (interactive).}

\item{output_dir}{Character. Directory to save results. Default is "outputs/maps".}

\item{save}{Logical. Whether to save the raster. Default is TRUE.}
}
\value{
A SpatRaster with NDVI values (one layer per month).
}
\description{
Calculates the Normalized Difference Vegetation Index (NDVI) for a given
country and region using MODIS NDVI monthly data via the geodata package.
NDVI = (NIR - Red) / (NIR + Red). Values range from -1 to 1,
where higher values indicate denser vegetation.
}
\examples{
\dontrun{
# Interactive mode
ndvi <- calculate_ndvi()

# Direct mode
ndvi <- calculate_ndvi(
  country = "MAR",
  region  = "Souss-Massa",
  year    = 2022,
  month   = c(6, 7, 8)
)
}

}
