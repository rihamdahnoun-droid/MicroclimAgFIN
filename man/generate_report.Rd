% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_report.R
\name{generate_report}
\alias{generate_report}
\title{Generate Microclimate Analysis Report}
\usage{
generate_report(
  lst = NULL,
  ndvi = NULL,
  anomalies = NULL,
  thermal_zones = NULL,
  model_results = NULL,
  spatial_corr = NULL,
  lst_ndvi = NULL,
  features = NULL,
  country = "Morocco",
  region = "Study Area",
  format = "html",
  output_dir = "outputs/reports",
  open_report = TRUE
)
}
\arguments{
\item{lst}{SpatRaster. Cleaned LST raster. Default is NULL.}

\item{ndvi}{SpatRaster. NDVI raster. Default is NULL.}

\item{anomalies}{List. Output from calculate_lst_anomaly(). Default is NULL.}

\item{thermal_zones}{List. Output from cluster_thermal_zones(). Default is NULL.}

\item{model_results}{List. Output from model_temperature_relationships(). Default is NULL.}

\item{spatial_corr}{List. Output from analyze_spatial_correlation(). Default is NULL.}

\item{lst_ndvi}{List. Output from compare_lst_vs_ndvi(). Default is NULL.}

\item{features}{Data frame. Output from extract_microclimate_features(). Default is NULL.}

\item{country}{Character. Country name for the report. Default is "Morocco".}

\item{region}{Character. Region name for the report. Default is "Study Area".}

\item{format}{Character. Output format: "html" or "pdf". Default is "html".}

\item{output_dir}{Character. Directory to save report. Default is "outputs/reports".}

\item{open_report}{Logical. Whether to open the report after generation. Default is TRUE.}
}
\value{
Invisibly returns the path to the generated report.
}
\description{
Generates an automatic HTML or PDF report summarizing all microclimate
analysis results including LST statistics, thermal anomalies, NDVI
comparison, spatial clusters, and model performance.
}
\examples{
\dontrun{
generate_report(
  lst           = lst_clean,
  ndvi          = ndvi,
  anomalies     = anomalies,
  thermal_zones = thermal_zones,
  model_results = model_results,
  country       = "Morocco",
  region        = "Tanger-Tetouan",
  format        = "html"
)
}

}
