% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_microclimate_features.R
\name{extract_microclimate_features}
\alias{extract_microclimate_features}
\title{Extract Microclimate Features}
\usage{
extract_microclimate_features(
  lst,
  ndvi,
  landcover = NULL,
  elevation = NULL,
  country = "MAR",
  output_dir = "outputs/tables",
  save = TRUE
)
}
\arguments{
\item{lst}{SpatRaster. Cleaned LST raster from clean_lst_data().}

\item{ndvi}{SpatRaster. NDVI raster from calculate_ndvi().}

\item{landcover}{SpatRaster. Land cover raster from import_landcover().}

\item{elevation}{SpatRaster. Elevation raster. If NULL, downloaded automatically.}

\item{country}{Character. ISO3 country code (needed if elevation is NULL).}

\item{output_dir}{Character. Directory to save results. Default is "outputs/tables".}

\item{save}{Logical. Whether to save the data frame. Default is TRUE.}
}
\value{
A data frame with extracted microclimate features per pixel
including coordinates (x, y), LST values per month, NDVI values per month,
elevation in meters, land cover class, mean LST, and mean NDVI.
}
\description{
Extracts and combines microclimate variables from multiple raster layers
(LST, NDVI, elevation, land cover) into a single data frame for analysis.
Performs zonal extraction and spatial aggregation.
}
\examples{
\dontrun{
features <- extract_microclimate_features(
  lst       = lst_clean,
  ndvi      = ndvi,
  landcover = lc,
  country   = "MAR"
)
}

}
